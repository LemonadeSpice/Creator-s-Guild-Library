Rendering technique used in computer graphics to create cutouts
It establishes a transparency threshold; any pixel with an alpha value below this threshold is completely discarded (cut away), while pixels above it remain fully opaque

It writes to the [[Depth Buffer|depth buffer]], meaning [[Easy-Z]] can be used for the opaque pixels